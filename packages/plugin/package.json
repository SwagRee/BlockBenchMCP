{
  "name": "@blockbench-mcp/plugin",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --platform=neutral --target=es2020 --outfile=dist/blockbench_mcp.js --legal-comments=none",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@blockbench-mcp/shared": "*",
    "zod": "^3.24.2",
    "zod-to-json-schema": "^3.24.5"
  },
  "devDependencies": {
    "blockbench-types": "5.1.0",
    "esbuild": "^0.25.1",
    "typescript": "^5.8.2"
  }
}
