{
  "name": "@blockbench-mcp/plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --platform=browser --target=es2020 --outfile=dist/blockbench_mcp.js --legal-comments=none",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@blockbench-mcp/shared": "*"
  },
  "devDependencies": {
    "blockbench-types": "5.1.0",
    "esbuild": "^0.25.1",
    "typescript": "^5.8.2"
  }
}
