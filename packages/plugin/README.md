# @blockbench-mcp/plugin

Blockbench desktop plugin. Build:

```bash
npm run build -w @blockbench-mcp/plugin
```

Load `dist/blockbench_mcp.js` in Blockbench (desktop). Match port/secret with the adapter.
