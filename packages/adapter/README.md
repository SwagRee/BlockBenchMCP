# @blockbench-mcp/adapter

stdio MCP server. Listens for the Blockbench plugin on loopback WebSocket.

```bash
npx blockbench-mcp
# or: node packages/adapter/dist/cli.js --port 39741 --secret your-secret
```

Env: `BLOCKBENCH_MCP_SECRET`, `BLOCKBENCH_MCP_PORT`.
